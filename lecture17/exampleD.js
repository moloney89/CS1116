document.addEventListener('DOMContentLoaded', init, false)

function init() {
    let heading_element = document.querySelector('h1');
    heading_element.className = "my_heading"; // changes class name
}

